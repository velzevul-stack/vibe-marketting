# Скопируйте в ignor_list.txt (рядом в этом каталоге) или положите ignor_list.txt в каталог пакета кампании.
# Один username на строку, с @ или без. Строки с # в начале — комментарии.
# Можно вставить CSV с колонкой Username (как в экспорте участников).

# example_user
